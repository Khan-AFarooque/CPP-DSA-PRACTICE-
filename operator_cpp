// opeatos in cpp
#include <iostream>
using namespace std;
int main(){
   
    int a=3,b=6;
     // logical opeator
    cout<<(a&&b)<<endl;
    cout<<(a||b)<<endl;
    cout<<(!b)<<endl;
    // relational opeator
    // aimetic
    // bitwise opertor

    cout<<(a & b)<<endl;  //bitwise and
    cout<<(a | b)<<endl;  //bitwise or
    cout<<(a ^ b)<<endl;  //bitwise xor
    cout<<(a << 1)<<endl;  //bitwise left shift
    cout<<(a >> 1)<<endl;  //bitwise right shift
    cout<<~(a)<<endl;  //complement -(a+1)

    // assignment opeators
    cout<<(a+=b)<<endl;
    cout<<(a+=b)<<endl;
    cout<<(a*=b)<<endl;
    cout<<(a/=b)<<endl;
    // coonditional/terney

    int result = (a<b)?b:a;
    cout<<"the greater number is "<<result;
    // sizeof
    char ch;
    cout<<sizeof(ch)<<endl;
    // address operator
    cout<<&a;
    // dot opeator
//     struct Student
// {
//     string name;
//     int age;
// };

// int main()
// {
//     Student s;

//     s.name = "Khan";
//     s.age = 20;

//     cout << s.name;
// }
 
// used with pointers to object
// struct student
// {
//     string name;

// };
// int main() 
// {
// student s;
// // first equate add 
// student *ptr=&s;
// ptr->name="khan";
// cout<<ptr->name;

// }
// cast operator
double pi=3.14;
int a=static_cast<int>(pi);
cout<<a;

    
    return 0;
}

